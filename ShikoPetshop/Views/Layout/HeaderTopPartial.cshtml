@using ShikoPetshop.Models;
<div class="agileits_header" data-aos="fade-down">
        <div class="w3l_offers">
            <a href="@Url.Action("Index","Home")">SHIKO</a>
        </div>
        <div class="w3l_search">
            @*<form action="#" method="post">*@
            @using(Html.BeginForm("KQTimKiem", "TimKiem"))
            {
                <input type="text" name="sTuKhoa" value="Tìm Kiếm..." onfocus="this.value = '';" onblur="if (this.value == '') {this.value = 'Search a product...';}" required="">
                <input type="submit" value=" ">
            }
            @*</form>*@
        </div>
    @*GIỎ HÀNG*@
        @Html.Action("GioHangPartial","GioHang")
        <div class="w3l_header_right">            
            <ul>
                @if (Session["TaiKhoan"] != null && Session["TaiKhoan"] != "")
                {
                    ThanhVien tv = (ThanhVien)Session["TaiKhoan"];
                    @*<li style="color:white;margin-top:13px">Chào @tv.TaiKhoan</li>*@
                    <li class="dropdown profile_details_drop">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="fa fa-2x fa-user" aria-hidden="true" style="margin-right:10px"></i>@tv.TaiKhoan<span class="caret"></span></a>
                        <div class="mega-dropdown-menu">
                            <div class="w3ls_vegetables">
                                <ul class="dropdown-menu drp-mnu">                                                           
                                    <li><i class="fa fa-power-off" aria-hidden="true" style="margin-right:10px"></i>@Html.ActionLink("Đăng Xuất", "DangXuat", "Home")</li>
                                </ul>
                            </div>
                        </div>
                    </li>
                }
                else
                {
                    <li class="dropdown profile_details_drop">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="fa fa-2x fa-user" aria-hidden="true"></i><span class="caret"></span></a>
                        <div class="mega-dropdown-menu">
                            <div class="w3ls_vegetables">
                                <ul class="dropdown-menu drp-mnu">
                                    <li><a data-target="#login" data-toggle="modal" href="#">Đăng Nhập</a></li>
                                    <li><a href="@Url.Action("DangKy","Home")">Đăng Kí</a></li>
                                </ul>
                            </div>
                        </div>
                    </li>
                }
            </ul>
        </div>
        <div class="w3l_header_right1">
            <h2><a href="#">PETSHOP</a></h2>
        </div>
        <div class="clearfix"> </div>
    </div>
@Html.Partial("~/Views/Layout/LoginModalPartial.cshtml");